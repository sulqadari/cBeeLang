- Добавить поддержку инкремента/декремента;

- Добавить поддержку массивов;

- Добавить поддержку switch-case.

- Отделить Scanner и Compiler от интерпретатора. Без этого будет невозможно
   мигрировать с ноутбука на STM32. Задумка в стиле "Java Card":
   компиляция на стороне компа, а на стороне STM32 только интерпретация.
   
- Если не ошибаюсь, то текущая реализация неразрывно связывает исходный
   код и сканируемый-компилируемый-интерпретируемый байт-код (см п. 16.2).
   Выглядит это следующим образом: при сканировании каждый токен должен
   иметь лексему. Автор вместо того, чтобы копировать для каждого токена
   соответствующую подстроку из исходного кода (что очень накладно с точки
   зрения кода) просто дает им ссылку на подстроку в исходном коде.
   Получается, что если нет исходного кода - нет лексемы, нет компиляции
   и интерпретации?
   Если так, то это надо переделать, иначе миграция будет невозможна.
   
- Язык НЕ должен быть динамически-типизируемым!
